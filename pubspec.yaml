dependencies:
  flutter:
    sdk: flutter

  # 데이터 저장 및 실시간 스트림 관리용
  supabase_flutter: ^2.8.1

  # 관리자 페이지 엑셀 다운로드용
  excel: ^4.0.0

  # 통계 및 날짜 포맷팅용 (NumberFormat, DateFormat)
  intl: ^0.19.0

  # 문자 메시지(SMS) 전송 기능용
  url_launcher: ^6.3.1
